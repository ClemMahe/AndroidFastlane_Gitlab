json_key_file("~/google_play_api_key.json")
#json_key_file will be different from local env to CI, commit once with CI path & then modify locally without committing
#also avoid committing Gemfile.lock & COPY if ruby>2.4 is not installed

#Path to json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name("com.clemmahe.goingout") # e.g. com.krausefx.app
